export enum TypeBankName {
  MBBank = 0,
}

/**
 * Trạng thái làm bài quiz
 */
export enum StatusMakeQuiz {
  require = "require",
  optional = "optional",
}

/**
 * Trạng thái truy cập quicktest
 */
export enum StatusAccess {
  public = "public",
  private = "private",
}
